---
import type { DogInfo } from '../../data/dog';

interface Props {
    dogInfo: DogInfo;
}

const { dogInfo } = Astro.props;
---

<section class="memory-lane">
    <div class="memory-container">
        <h2 class="memory-title">Our Journey Together</h2>
        <div class="memory-content">
            <p>
                {dogInfo.name} has brought so much joy and happiness into our lives. From the first day we met, it was clear that {dogInfo.name} was going to be something special.
                {dogInfo.nickname} (as we sometimes call {dogInfo.name.toLowerCase()}) has a way of making every day brighter with his {dogInfo.personality[0].toLowerCase()} personality and unconditional love.
            </p>
            <p>
                Whether {dogInfo.name} is {dogInfo.favoriteThings[0].toLowerCase()} or simply enjoying a quiet moment at home, every experience with {dogInfo.name} is a reminder of how precious the bond between humans and dogs can be.
            </p>
            <p>
                These carefully selected photos capture the essence of {dogInfo.name}'s spirit - from playful moments in the park to peaceful cuddle sessions, each image tells a story of love, companionship, and the pure joy that {dogInfo.name} brings to our lives every single day.
            </p>
        </div>
    </div>
</section>

<style>
    .memory-lane {
        margin-bottom: var(--spacing-4xl);
    }

    .memory-container {
        background: linear-gradient(
            135deg,
            var(--color-accent) 0%,
            var(--color-accent-dark) 100%
        );
        color: white;
        border-radius: var(--radius-2xl);
        padding: var(--spacing-3xl) var(--spacing-xl);
        box-shadow: var(--shadow-xl);
        max-width: var(--container-xl);
        margin: 0 auto;
        position: relative;
        overflow: hidden;
    }

    .memory-container::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.1'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
        opacity: 0.5;
    }

    .memory-title {
        text-align: center;
        font-size: var(--font-size-4xl);
        margin-bottom: var(--spacing-xl);
        font-weight: var(--font-weight-bold);
        text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
        position: relative;
        z-index: 1;
    }

    .memory-content {
        max-width: 800px;
        margin: 0 auto;
        font-size: var(--font-size-lg);
        line-height: var(--line-height-relaxed);
        position: relative;
        z-index: 1;
    }

    .memory-content p {
        margin-bottom: var(--spacing-xl);
        text-align: center;
        opacity: 0.95;
    }

    .memory-content strong {
        color: white;
        font-weight: var(--font-weight-semibold);
    }

    @media (max-width: var(--breakpoint-md)) {
        .memory-container {
            padding: var(--spacing-2xl) var(--spacing-lg);
        }

        .memory-title {
            font-size: var(--font-size-3xl);
            margin-bottom: var(--spacing-lg);
        }

        .memory-content {
            font-size: var(--font-size-base);
        }

        .memory-content p {
            margin-bottom: var(--spacing-lg);
        }
    }

    @media (max-width: var(--breakpoint-sm)) {
        .memory-lane {
            margin-bottom: var(--spacing-3xl);
        }

        .memory-container {
            padding: var(--spacing-xl) var(--spacing-md);
            border-radius: var(--radius-xl);
        }

        .memory-title {
            font-size: var(--font-size-2xl);
        }

        .memory-content p {
            font-size: var(--font-size-sm);
        }
    }
</style>
